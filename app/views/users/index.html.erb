<h1 align="center"> List of all the Users</h1>

<% @user.each do |user| %>
	 	<div class="row">
	 		<div class="col-xs-8 col-xs-offset-2">
	 			<div class="well well-lg">
	 				<div class="username">
	 					<%= link_to user.username, user_path(user.id)  %>
	 				</div>
	 			</div>
	 		</div>
	 	</div>
 <% end %>
